// Generated by CoffeeScript 1.10.0
(function() {
  var Model, Sequelize, _, createOrUpdate, createRelated, handleEntityNotFound, handleError, removeEntity, responseWithResult, saveUpdates;

  Model = require("../../lib/model");

  Sequelize = require("../../lib/dbconn");

  _ = require("underscore");

  handleError = function(res, statusCode) {
    statusCode = statusCode || 500;
    return function(err) {
      console.log("error:" + err);
      console.log("error stack:" + err.stack);
      return res.status(statusCode).send(err);
    };
  };

  handleEntityNotFound = function(res) {
    return function(entity) {
      if (!entity) {
        res.status(404).end();
        return null;
      }
      return entity;
    };
  };

  responseWithResult = function(res, statusCode) {
    statusCode = statusCode || 200;
    return function(entity) {
      if (entity) {
        return res.status(statusCode).json(entity);
      }
    };
  };

  createOrUpdate = function(entity, updates) {
    var ids, ids2;
    ids = _.map(updates.stations, function(item) {
      return item.id;
    });
    ids2 = ids.join(",");
    if (ids2 === "") {
      ids2 = "0";
    }
    return Model.Station.findAll({
      where: {
        id: {
          "in": ids
        }
      },
      order: [Sequelize.literal("FIELD(id, " + ids2 + ")")]
    }).then(function(stations) {
      entity.setStations([]);
      return stations;
    }).then(function(stations) {
      return entity.setStations(stations);
    }).then(function() {
      var tids, tids2;
      tids = _.map(updates.StartTime, function(item) {
        return item.id;
      });
      tids2 = tids.join(",");
      if (tids2 === "") {
        tids2 = "0";
      }
      return Model.Starttime.findAll({
        where: {
          id: {
            "in": tids
          }
        },
        order: [Sequelize.literal("FIELD(id, " + tids2 + ")")]
      });
    }).then(function(starttimes) {
      entity.setStartTime([]);
      return starttimes;
    }).then(function(starttimes) {
      return entity.setStartTime(starttimes);
    }).then(function() {
      var pids, pids2;
      pids = _.map(updates.phones, function(item) {
        return item.id;
      });
      pids2 = pids.join(",");
      if (pids2 === "") {
        pids2 = "0";
      }
      return Model.Phone.findAll({
        where: {
          id: {
            "in": pids
          }
        },
        order: [Sequelize.literal("FIELD(id, " + pids2 + ")")]
      });
    }).then(function(phones) {
      entity.setPhones([]);
      return phones;
    }).then(function(phones) {
      entity.setPhones(phones);
      return updates;
    });
  };

  createRelated = function(updates) {
    return function(entity) {
      return createOrUpdate(entity, updates).then(function() {
        return updates;
      });
    };
  };

  saveUpdates = function(updates) {
    return function(entity) {
      var updatedOut;
      updatedOut = 0;
      return entity.update(updates).then(function(updated) {
        updatedOut = updated;
        return createOrUpdate(entity, updated);
      }).then(function() {
        return updatedOut;
      });
    };
  };

  removeEntity = function(res) {
    return function(entity) {
      if (entity) {
        return entity.destroy().then(function() {
          return res.status(204).end();
        });
      }
    };
  };

  module.exports.index = function(req, res) {
    return Model.Busline.findAll({
      include: [
        {
          model: Model.City,
          as: 'startCity'
        }, {
          model: Model.City,
          as: 'endCity'
        }, Model.Station, Model.Company
      ]
    }).then(responseWithResult(res))["catch"](handleError(res));
  };

  module.exports.show = function(req, res) {
    return Model.Busline.findOne({
      where: {
        id: req.params.id
      },
      include: [
        {
          model: Model.City,
          as: 'startCity'
        }, {
          model: Model.City,
          as: 'endCity'
        }, Model.Station, Model.Company, Model.Phone, {
          model: Model.Starttime,
          as: 'StartTime'
        }
      ]
    }).then(handleEntityNotFound(res)).then(responseWithResult(res))["catch"](handleError(res));
  };

  module.exports.update = function(req, res) {
    return Model.Busline.findOne({
      where: {
        id: req.params.id
      },
      include: [
        {
          model: Model.City,
          as: 'startCity'
        }, {
          model: Model.City,
          as: 'endCity'
        }, Model.Station, Model.Company, Model.Phone, {
          model: Model.Starttime,
          as: 'StartTime'
        }
      ]
    }).then(handleEntityNotFound(res)).then(saveUpdates(req.body)).then(responseWithResult(res))["catch"](handleError(res));
  };

  module.exports.create = function(req, res) {
    return Model.Busline.create(req.body).then(createRelated(req.body)).then(responseWithResult(res, 201))["catch"](handleError(res));
  };

  module.exports.destroy = function(req, res) {
    if (req.body.id) {
      delete req.body.id;
    }
    return Model.Busline.findOne({
      where: {
        id: req.params.id
      }
    }).then(handleEntityNotFound(res)).then(removeEntity(res))["catch"](handleError(res));
  };

}).call(this);

//# sourceMappingURL=buslines.controller.js.map
