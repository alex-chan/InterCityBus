// Generated by CoffeeScript 1.10.0
(function() {
  var ast, warnings;

  require('./setup');

  ast = void 0;

  warnings = void 0;

  describe('Warnings', function() {
    describe('with no warnings', function() {
      beforeEach(function() {
        var ref;
        return ref = js2coffee.build('var a'), ast = ref.ast, warnings = ref.warnings, ref;
      });
      it('produces an array', function() {
        return expect(warnings).be.an('array');
      });
      return it('produces an empty array', function() {
        return expect(warnings).have.length(0);
      });
    });
    return describe('with 1 warning', function() {
      beforeEach(function() {
        var opts, ref;
        opts = {
          filename: 'x.js'
        };
        return ref = js2coffee.build('function add() { var add }', opts), ast = ref.ast, warnings = ref.warnings, ref;
      });
      it('produces an array', function() {
        return expect(warnings).be.an('array');
      });
      it('has 1 warning', function() {
        return expect(warnings).have.length(1);
      });
      it('complains about shadowing', function() {
        expect(warnings[0].description).match(/'add'/);
        return expect(warnings[0].description).match(/shadow/);
      });
      it('has start marker', function() {
        return expect(warnings[0].start).eql({
          line: 1,
          column: 21
        });
      });
      it('has end marker', function() {
        return expect(warnings[0].end).eql({
          line: 1,
          column: 24
        });
      });
      return it('has filename', function() {
        return expect(warnings[0].filename).eql('x.js');
      });
    });
  });

}).call(this);

//# sourceMappingURL=warnings.js.map
