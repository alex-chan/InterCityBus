// Generated by CoffeeScript 1.10.0
(function() {
  require('./setup');

  describe('Precedence', function() {
    var getPrecedence, test;
    getPrecedence = require('../lib/helpers').getPrecedence;
    test = function(js, level) {
      return it("[" + level + "] " + js, function() {
        var node, result;
        node = require('esprima').parse(js).body[0].expression;
        result = getPrecedence(node);
        if (result !== level) {
          console.log(node);
        }
        return expect(result).eq(level);
      });
    };
    test('a * 2', 14);
    test('a + 2', 13);
    test('a << 2', 12);
    test('a >= b', 11);
    test('a instanceof b', 11);
    test('a != b', 10);
    test('a & b', 9);
    test('a ^ b', 8);
    test('a | b', 7);
    test('a && b', 6);
    test('a || b', 5);
    test('void 0', 15);
    test('++a', 15);
    test('--a', 15);
    test('a++', 16);
    test('a--', 16);
    return test('a.b', 18);
  });

}).call(this);

//# sourceMappingURL=precedence.js.map
