// Generated by CoffeeScript 1.10.0

/*
 * Generates spec notes (special_cases.md), invoked via a Makefile
 */

(function() {
  var eachGroup;

  eachGroup = require('./specs_iterator').eachGroup;

  eachGroup(function(group) {
    var i, len, results, spec, specs;
    if (group.pending) {
      return;
    }
    specs = group.specs.filter(function(s) {
      var ref;
      return (ref = s.meta) != null ? ref.notes : void 0;
    });
    if (specs.length === 0) {
      return;
    }
    console.log("## " + group.name + "\n");
    results = [];
    for (i = 0, len = specs.length; i < len; i++) {
      spec = specs[i];
      console.log("### " + spec.name + "\n");
      console.log(spec.meta.notes + "\n");
      console.log("```js\n// Input:\n" + spec.input + "```\n");
      results.push(console.log("```coffee\n# Output:\n" + spec.output + "```\n"));
    }
    return results;
  });

}).call(this);

//# sourceMappingURL=report_notes.js.map
